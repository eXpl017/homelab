apiVersion: 1

# deleteDatasources:
#   - name: Prometheus
#     orgId: 1

prune: true

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    uid: prom-datasource
    url: http://prometheus:9090
    version: 1
    editable: false
